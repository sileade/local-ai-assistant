# Техническое резюме проекта Scoliologic AI (sclg-ai)

**Scoliologic AI (sclg-ai)** — это автономный DevOps/SysAdmin агент и персональный ассистент, работающий на Mac Mini M4 Pro. Проект объединяет возможности локальных LLM (через Ollama) и облачных API (Claude) для управления инфраструктурой, мониторинга, анализа данных и автоматизации рутинных задач.

Главная философия проекта: **"Execute first, explain later"** (Сначала выполни, потом объясни). Агент самостоятельно собирает данные с системы перед тем, как отправить запрос в нейросеть, что исключает ответы вида "у меня нет доступа к вашей системе".

---

## 1. Архитектура и компоненты

Проект состоит из трех основных компонентов (Python-скриптов), которые работают в единой экосистеме:

### 1.1. Консольный агент (`sclg_ai_console.py`)
Основной интерактивный интерфейс в стиле Claude Code.
*   **SmartExecutor (Execute-First):** Перехватывает запросы пользователя (например, "какой у меня IP" или "покажи запущенные процессы"), локально выполняет соответствующие bash-команды на Mac Mini и передает сырой вывод (stdout/stderr) в нейросеть для анализа.
*   **ExpertRouter (MoE - Mixture of Experts):** Анализирует запрос по ключевым словам и направляет его профильному "эксперту" (sysadmin, code, analysis, creative, general). Каждому эксперту назначен свой системный промпт, температура генерации и список предпочтительных моделей.
*   **DreamMemory:** Двухуровневая система памяти. Краткосрочная память хранит контекст текущей сессии (последние 6 сообщений). Долгосрочная память автоматически извлекает и сохраняет важные факты (IP-адреса, ошибки, конфигурации) для использования в будущих сессиях.
*   **ResponseCleaner:** Пост-процессор ответов. Удаляет типичные "AI-штампы" (например, "Конечно!", "Вот ваш ответ:"), вырезает служебные токены моделей (`<think>`, `<|im_start|>`) и форматирует вывод в красивые таблицы с рамками и цветовой подсветкой.
*   **Claude Fallback:** Если локальная модель (Ollama) не справляется, выдает ошибку или дает слишком общий ответ, система автоматически переключается на Claude Sonnet 4.

### 1.2. Фоновый аналитик (`sclg_infra_learner.py`)
Демон для непрерывного мониторинга инфраструктуры.
*   **Сбор данных (Collector):** Регулярно опрашивает Grafana API, Prometheus и Loki. Собирает метрики серверов, GPU, сетевого оборудования (MikroTik) и камер (Hikvision).
*   **Анализ (Analyzer):** Использует LLM для поиска аномалий в собранных метриках и формирования "инсайтов" (InfraInsights).
*   **База знаний (KnowledgeBase):** Сохраняет базовые линии (baselines) нормального поведения системы и историю аномалий в JSON-файлы. Консольный агент использует эту базу для контекста при ответах пользователю.

### 1.3. Telegram-бот (`sclg_telegram_bot.py`)
Мобильный интерфейс для управления агентом.
*   **Персональный ассистент:** Интеграция с macOS через AppleScript (`osascript`). Позволяет создавать события в Календаре, задачи в Напоминаниях (Reminders), заметки (Apple Notes) и искать контакты.
*   **DevOps-функции:** Доступ к выполнению команд, сканированию сети и SSH-подключениям прямо из Telegram.
*   **Голосовой ввод:** Транскрибация голосовых сообщений через локальную модель Whisper.
*   **Контроль доступа и биллинг:** Строгая авторизация по Telegram ID и система отслеживания затрат (CostTracker) на использование платных API (Claude) с дневными и месячными лимитами.

---

## 2. Инфраструктура и развертывание

Система спроектирована для работы в распределенной среде с балансировкой нагрузки на GPU.

### 2.1. Топология сети
| Узел | IP-адрес | Роль |
| :--- | :--- | :--- |
| **mac-mini** | `172.27.4.255` | Главный хост агента (Apple M4 Pro). Запускает Python-скрипты. |
| **ai-server** | `10.0.0.229` | GPU Balancer. Распределяет запросы к локальным моделям. |
| **ai002** | `172.27.5.114` | Вычислительный узел (2x RTX 5070 Ti). |
| **ai012** | `172.27.5.150` | Вычислительный узел (RTX 2000 Ada). |
| **ai003** | `172.27.4.242` | Вычислительный узел (RTX 5070 Ti). |
| **grafana** | `172.27.5.111` | Сервер мониторинга (Grafana, Prometheus, Loki). |
| **bastion** | `45.144.43.100` | VPN/SSH шлюз для удаленного доступа и деплоя. |

### 2.2. CI/CD (Автоматическое развертывание)
Проект использует GitHub Actions для автоматического деплоя при пуше в ветку `main`.
1.  **Проверка синтаксиса:** `py_compile` проверяет Python-файлы на ошибки.
2.  **Доставка через Bastion:** Файлы копируются на bastion-сервер (`45.144.43.100:2222`) с использованием `sshpass`.
3.  **Установка на Mac Mini:** С bastion-сервера файлы копируются на Mac Mini (`172.27.4.255`), помещаются в `/opt/homebrew/bin/` и `~/bin/`, и им назначаются права на выполнение (`chmod +x`).

---

## 3. Инструкция по быстрому созданию и подключению нейросетей

Архитектура sclg-ai позволяет легко добавлять новые локальные модели или подключать сторонние API.

### 3.1. Подключение новой локальной модели (Ollama)
Все локальные модели управляются через Ollama, работающую на GPU Balancer (`10.0.0.229`).

1.  **Загрузка модели на кластер:**
    Подключитесь к `ai-server` и скачайте нужную модель:
    ```bash
    ollama pull deepseek-coder:33b
    ```
2.  **Добавление модели в маршрутизатор (ExpertRouter):**
    Откройте файл `sclg_ai_console.py` и найдите словарь `MODEL_PROFILES` (строка ~175). Добавьте имя скачанной модели в список `models` нужного эксперта. Модели в начале списка имеют высший приоритет.
    ```python
    "code": {
        "models": ["deepseek-coder:33b", "sclg-coder:32b", "qwen2.5-coder:14b"],
        "keywords": ["код", "python", "bash", "ошибка", "bug"],
        "temperature": 0.2,
        "system_hint": "Ты опытный программист. Пиши чистый, рабочий код.",
    }
    ```
3.  **Применение изменений:**
    Запушьте изменения в GitHub (сработает автодеплой) или скопируйте файл на Mac Mini вручную.

### 3.2. Подключение нового облачного API (например, OpenAI или DeepSeek API)
Если вы хотите добавить поддержку нового API (в дополнение к Claude), необходимо создать новый класс-клиент по аналогии с `ClaudeClient`.

1.  **Добавление переменных окружения:**
    На Mac Mini добавьте ключ API в `~/.zshrc` или `~/.bash_profile`:
    ```bash
    export OPENAI_API_KEY="sk-your-key-here"
    ```
2.  **Создание клиента в коде:**
    В `sclg_ai_console.py` создайте класс `OpenAIClient`:
    ```python
    class OpenAIClient:
        def __init__(self):
            self.api_key = os.environ.get("OPENAI_API_KEY", "")
            self.api_url = "https://api.openai.com/v1/chat/completions"
            self.model = "gpt-4o"

        def test_connection(self):
            return bool(self.api_key)

        def chat(self, messages, system=""):
            if not self.api_key: return "[ERROR] API key missing"
            
            # Форматирование сообщений под формат OpenAI
            formatted_msgs = [{"role": "system", "content": system}] + messages
            
            headers = {
                "Authorization": f"Bearer {self.api_key}",
                "Content-Type": "application/json"
            }
            data = json.dumps({
                "model": self.model,
                "messages": formatted_msgs,
                "temperature": 0.3
            }).encode("utf-8")
            
            try:
                req = urllib.request.Request(self.api_url, data=data, headers=headers)
                with urllib.request.urlopen(req, timeout=30) as resp:
                    result = json.loads(resp.read().decode())
                    return result["choices"][0]["message"]["content"]
            except Exception as e:
                return f"[ERROR] OpenAI API: {e}"
    ```
3.  **Интеграция в главный класс `SclgAI`:**
    *   Инициализируйте клиента в `__init__`: `self.openai = OpenAIClient()`
    *   Добавьте проверку в `check_connections()`
    *   Измените логику fallback в `_get_ai_response()`, чтобы использовать новый клиент при недоступности локальных моделей.

### 3.3. Добавление новых команд (Smart Patterns)
Чтобы агент научился автоматически собирать новые данные перед ответом, добавьте паттерн в список `SMART_PATTERNS` в `sclg_ai_console.py`.

Формат: `(["ключевые", "слова"], ["bash_команда_1", "bash_команда_2"], "категория_эксперта")`

Пример добавления проверки Docker:
```python
    (["докер", "docker", "контейнеры", "containers"],
     ["docker ps -a 2>/dev/null",
      "docker stats --no-stream 2>/dev/null | head -10"],
     "sysadmin"),
```
Теперь при запросе "что там с докером?" агент сначала выполнит `docker ps` и `docker stats`, а затем передаст результаты нейросети для анализа.
